# Node.js App

A simple Node.js application created for the DevOps course activity.

## Getting Started

1. Clone the repository
2. Run `npm install` to install dependencies
3. Run `npm start` to start the server

The app will be available at http://localhost:3000

## CI/CD

This project uses GitHub Actions for continuous integration. The workflow runs tests on push and pull requests to the main branch.